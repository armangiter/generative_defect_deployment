version: "3.8"

services:
  nginx:
    build: .
    command: "npm run dev"
    ports:
      - "82:82"
    volumes:
      - static_volume:/home/app/microservice/static
    restart: always
    networks:
      - host

networks:
  generative-network:

volumes:
  static_volume: